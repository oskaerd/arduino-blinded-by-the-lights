#define RED_IDX     0
#define GREEN_IDX   1
#define BLUE_IDX    2


uint32_t SerialMusic(uint32_t iteration)
{
    uint8_t readData[3];



    return DELAYVAL;
}

        // uint8_t result[2] = {0};
        // Serial.readBytes(result, 2);
        // Serial.write(result, 2);

        // if (result[0] <= 30)
        // {
        //   red += result[0];
        // }
        // else
        // {
        //     red -= (result[0] - 30);
        // }

        // if (result[1] <= 30)
        // {
        //   blue += result[1];
        // }
        // else
        // {
        //     blue -= (result[1] - 30);
        // }
